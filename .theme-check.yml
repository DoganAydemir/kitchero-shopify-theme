extends: theme-check:recommended

# Keep MatchingTranslations disabled — we deliberately maintain a staged
# locale rollout; non-English files can lag the English source during porting.
# Re-enable before Theme Store submission.
MatchingTranslations:
  enabled: false
