extends: theme-check:recommended

# MatchingTranslations re-enabled — the full EN/TR/DE/ES/FR parity sweep
# (storefront .json + schema .json) is complete and verified. Re-enable
# so CI catches any future locale drift before it reaches merchants.
MatchingTranslations:
  enabled: true
