{{ 'section-customer-forms.css' | asset_url | stylesheet_tag }}

<section class="kt-customer" data-section-id="{{ section.id }}">
  <div class="kt-customer__container">
    <h1 class="kt-customer__title">{{ 'customers.reset_password.title' | t }}</h1>
    <p class="kt-customer__subtitle">{{ 'customers.reset_password.subtext' | t }}</p>

    {%- form 'reset_customer_password', id: 'ResetPasswordForm' -%}
      {%- if form.errors -%}
        <div class="kt-customer__errors" role="alert" tabindex="-1" autofocus>
          <h2 class="kt-customer__errors-title">{{ 'templates.contact.form.error_heading' | t }}</h2>
          <ul class="kt-customer__errors-list">
            {%- for field in form.errors -%}
              <li>
                {%- if field == 'form' -%}
                  {{ form.errors.messages[field] }}
                {%- else -%}
                  <a href="#reset-{{ field }}">
                    {{ form.errors.translated_fields[field] | capitalize }} {{ form.errors.messages[field] }}
                  </a>
                {%- endif -%}
              </li>
            {%- endfor -%}
          </ul>
        </div>
      {%- endif -%}

      <div class="kt-customer__form">
        <div class="kt-customer__field">
          <label for="reset-password" class="kt-customer__label">
            {{ 'customers.reset_password.password' | t }}
          </label>
          <input
            type="password"
            id="reset-password"
            name="customer[password]"
            class="kt-customer__input"
            required
            autocomplete="new-password"
            aria-required="true"
          >
        </div>

        <div class="kt-customer__field">
          <label for="reset-password-confirm" class="kt-customer__label">
            {{ 'customers.reset_password.password_confirm' | t }}
          </label>
          <input
            type="password"
            id="reset-password-confirm"
            name="customer[password_confirmation]"
            class="kt-customer__input"
            required
            autocomplete="new-password"
            aria-required="true"
          >
        </div>

        <button type="submit" class="kt-customer__submit">
          {{ 'customers.reset_password.submit' | t }}
        </button>
      </div>
    {%- endform -%}
  </div>
</section>

{% schema %}
{
  "name": "t:sections.main-reset-password.name",
  "tag": "section",
  "class": "section",
  "settings": []
}
{% endschema %}
